{
  "name": "joshternet-http-message-signatures-directory",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "http-message-sig": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.132.0"
  }
}
